{
    "testMode": true,
    "default": {
        "data":{
            "name":"data.json",
            "description":"在选择了某文件夹后，会自动寻找对应name的json文件,并解析，为什么是选择文件夹？这是个历史遗留问题，但是不改问题也不大，而且可扩展性变强了"
        },
        "scene": {
            "description": "默认的场景",
            "path": "Assets/AssetArt/Scene/SceneUI.unity"
        },
        "prefab": {
            "description": "默认的prefab的位置和名字",
            "path": "Assets/AssetArt/Prefabs",
            "name": "test.prefab"
        },
        "screen": {
            "description": "默认的屏幕长宽,如果不设置这个,会默认采用PS中PSD的width和height,如果不想设置这个,可以设置为0,0作为不采用的值",
            "width": 1080,
            "height": 2340
        },
        "font": {
            "enableCorrect": true,
            "CorrectValue": 0.75,
            "description": "Unity 中文字字号是“理论值”，实际显示时会因为字体边界、内边距、贴图放缩、Canvas 缩放等机制，导致视觉效果比 Photoshop 更大、更松。故而需要进行比例修正"
        }
    },
    "fontAssets": {
        "default": {
            "description": "默认字体",
            "path": "Assets/AssetRaw/Fonts/Microsoft_black_Bbold.ttf"
        }
    }
}